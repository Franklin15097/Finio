# 🚀 Инструкция по деплою обновлений

## ✅ Изменения закоммичены и запушены в GitHub

Все изменения уже находятся в репозитории:
- Улучшена цветовая схема для финансового приложения
- FAB кнопки подняты выше (bottom-32 вместо bottom-24)
- Модальные окна теперь отображаются в центре экрана
- Обновлены градиенты и цвета

## 📋 Для деплоя на сервер выполните:

### Вариант 1: Автоматический деплой (рекомендуется)

Подключитесь к серверу и запустите скрипт обновления:

```bash
ssh root@85.235.205.99
cd /var/www/studiofinance
./update_server.sh
```

### Вариант 2: Ручной деплой

Если автоматический скрипт не работает, выполните команды вручную:

```bash
# 1. Подключитесь к серверу
ssh root@85.235.205.99

# 2. Перейдите в директорию проекта
cd /var/www/studiofinance

# 3. Получите последние изменения
git pull origin main

# 4. Обновите и соберите frontend
cd frontend
npm install
npm run build
cd ..

# 5. Обновите и соберите backend
cd backend
npm install
npm run build
cd ..

# 6. Перезапустите backend
pm2 restart finio-backend

# 7. Проверьте статус
pm2 status

# 8. Посмотрите логи (опционально)
pm2 logs finio-backend --lines 50
```

## 🔍 Проверка после деплоя

1. **Откройте сайт**: https://studiofinance.ru
2. **Проверьте Telegram Mini App**: https://t.me/FinanceStudio_bot/app
3. **Проверьте изменения**:
   - Цветовая схема стала более насыщенной и контрастной
   - FAB кнопки (кнопки с плюсом) находятся выше нижней панели
   - Модальные окна открываются в центре экрана

## 📊 Полезные команды

### Проверка статуса сервисов
```bash
pm2 status                    # Статус PM2 процессов
systemctl status nginx        # Статус Nginx
systemctl status mysql        # Статус MySQL
```

### Просмотр логов
```bash
pm2 logs finio-backend        # Логи backend в реальном времени
pm2 logs finio-backend --lines 100  # Последние 100 строк
tail -f /var/log/nginx/error.log    # Логи Nginx
```

### Перезапуск сервисов
```bash
pm2 restart finio-backend     # Перезапуск backend
systemctl reload nginx        # Перезагрузка Nginx
pm2 restart all              # Перезапуск всех PM2 процессов
```

## 🐛 Решение проблем

### Если сайт не обновился
```bash
# Очистите кеш браузера или откройте в режиме инкогнито
# Проверьте, что frontend собрался:
ls -la /var/www/studiofinance/frontend/dist
```

### Если backend не запускается
```bash
pm2 logs finio-backend --err  # Посмотрите ошибки
pm2 restart finio-backend     # Попробуйте перезапустить
```

### Если нужно откатить изменения
```bash
cd /var/www/studiofinance
git log --oneline -5          # Посмотрите последние коммиты
git reset --hard <commit-hash> # Откатитесь к нужному коммиту
./update_server.sh            # Пересоберите проект
```

## 📝 Информация о сервере

- **IP**: 85.235.205.99
- **Домен**: https://studiofinance.ru
- **Проект**: /var/www/studiofinance
- **Backend процесс**: finio-backend (PM2)
- **База данных**: MySQL (financial_db)

## ✨ Что изменилось в этом обновлении

### Цветовая схема
- Обновлены CSS переменные для более профессионального вида
- Улучшены цвета для доходов (зеленый) и расходов (красный)
- Более контрастные и насыщенные градиенты

### UX улучшения
- FAB кнопки подняты с `bottom-24` на `bottom-32` для лучшей доступности
- Модальные окна теперь центрируются на всех устройствах
- Добавлена анимация `animate-scale-in` для модальных окон

### Затронутые файлы
- `frontend/src/index.css` - обновлены CSS переменные и градиенты
- `frontend/src/components/Modal.tsx` - центрирование модальных окон
- `frontend/src/pages/telegram/TelegramAccounts.tsx` - позиция FAB кнопки
- `frontend/src/pages/telegram/TelegramIncome.tsx` - позиция FAB кнопки
- `frontend/src/pages/telegram/TelegramExpenses.tsx` - позиция FAB кнопки

---

**Последнее обновление**: $(date)
**Коммит**: 14293fe - "Добавлен скрипт обновления для сервера"
