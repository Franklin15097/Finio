# 📜 Скрипты развертывания Finio

Этот каталог содержит скрипты для автоматизации установки и развертывания проекта Finio.

---

## 📋 Доступные скрипты

### 1. 🚀 full_server_setup.sh
**Полная автоматическая установка на чистый сервер**

Выполняет полную установку проекта с нуля:
- Очистка сервера от предыдущих установок
- Установка всех зависимостей (Node.js, MySQL, Redis, Nginx, PM2)
- Настройка базы данных
- Клонирование и развертывание проекта
- Настройка SSL сертификатов
- Настройка firewall и безопасности
- Настройка автоматических бэкапов

**Использование:**
```bash
# 1. Скачать скрипт
wget https://raw.githubusercontent.com/Franklin15097/Finio/main/scripts/full_server_setup.sh

# 2. Отредактировать конфигурацию
nano full_server_setup.sh
# Укажите: DOMAIN, API_DOMAIN, TELEGRAM_BOT_TOKEN, SSL_EMAIL

# 3. Запустить установку
chmod +x full_server_setup.sh
sudo ./full_server_setup.sh
```

**Время выполнения:** 15-30 минут

**Требования:**
- Ubuntu Server 20.04+
- Права root/sudo
- 2 домена с настроенными DNS записями
- Telegram Bot Token
- Email для SSL

📖 **Подробная инструкция:** [docs/SERVER_SETUP_GUIDE.md](../docs/SERVER_SETUP_GUIDE.md)

---

### 2. 🔄 deploy.sh
**Автоматический деплой с локальной машины**

Выполняет деплой изменений на production сервер:
- Проверка незакоммиченных изменений
- Push в GitHub
- SSH подключение к серверу
- Pull последних изменений
- Сборка frontend и backend
- Перезапуск сервисов

**Использование:**
```bash
# Полный деплой (frontend + backend + bot)
./scripts/deploy.sh full

# Только frontend
./scripts/deploy.sh frontend

# Только backend
./scripts/deploy.sh backend

# Только bot
./scripts/deploy.sh bot
```

**Требования:**
- SSH доступ к серверу
- Настроенный SSH ключ
- Git репозиторий

**Конфигурация:**
Отредактируйте переменные в начале скрипта:
```bash
SERVER_USER="root"
SERVER_HOST="85.235.205.99"
SERVER_PATH="/var/www/studiofinance"
```

---

### 3. ⚡ update_server.sh
**Быстрое обновление на сервере**

Выполняет быстрое обновление приложения непосредственно на сервере:
- Pull последних изменений из GitHub
- Обновление зависимостей
- Пересборка frontend и backend
- Перезапуск сервисов

**Использование:**
```bash
# На сервере
cd /var/www/studiofinance
./scripts/update_server.sh
```

**Время выполнения:** 2-5 минут

**Когда использовать:**
- Для быстрых обновлений без локальной машины
- Когда изменения уже в GitHub
- Для автоматизации через cron

---

## 🔧 Сравнение скриптов

| Скрипт | Назначение | Где запускать | Время | Сложность |
|--------|-----------|---------------|-------|-----------|
| **full_server_setup.sh** | Первоначальная установка | На сервере | 15-30 мин | Высокая |
| **deploy.sh** | Регулярный деплой | На локальной машине | 3-7 мин | Средняя |
| **update_server.sh** | Быстрое обновление | На сервере | 2-5 мин | Низкая |

---

## 📝 Типичные сценарии использования

### Сценарий 1: Первая установка
```bash
# 1. На сервере
wget https://raw.githubusercontent.com/Franklin15097/Finio/main/scripts/full_server_setup.sh
nano full_server_setup.sh  # Настроить параметры
chmod +x full_server_setup.sh
sudo ./full_server_setup.sh
```

### Сценарий 2: Разработка и деплой
```bash
# 1. На локальной машине - разработка
git add .
git commit -m "feat: новая функция"

# 2. Деплой на production
./scripts/deploy.sh full
```

### Сценарий 3: Быстрое обновление
```bash
# 1. На локальной машине - push изменений
git push origin main

# 2. На сервере - обновление
cd /var/www/studiofinance
./scripts/update_server.sh
```

---

## 🛠️ Настройка скриптов

### Переменные окружения

Все скрипты используют следующие переменные:

**full_server_setup.sh:**
```bash
DOMAIN="studiofinance.ru"
API_DOMAIN="api.studiofinance.ru"
TELEGRAM_BOT_TOKEN="your_bot_token"
SSL_EMAIL="your-email@example.com"
PROJECT_PATH="/var/www/studiofinance"
REPO_URL="https://github.com/Franklin15097/Finio.git"
```

**deploy.sh:**
```bash
SERVER_USER="root"
SERVER_HOST="85.235.205.99"
SERVER_PATH="/var/www/studiofinance"
BACKEND_SERVICE="finio-backend"
BOT_SERVICE="finio-bot"
```

**update_server.sh:**
```bash
PROJECT_DIR="/var/www/studiofinance"
```

---

## 🔒 Безопасность

### Рекомендации:
1. ✅ Используйте SSH ключи вместо паролей
2. ✅ Храните токены и пароли в безопасном месте
3. ✅ Не коммитьте .env файлы в Git
4. ✅ Регулярно обновляйте зависимости
5. ✅ Проверяйте логи после деплоя

### Настройка SSH ключа:
```bash
# На локальной машине
ssh-keygen -t ed25519 -C "your-email@example.com"
ssh-copy-id root@your-server-ip

# Проверка
ssh root@your-server-ip
```

---

## 🐛 Устранение неполадок

### Проблема: Скрипт не запускается
```bash
# Решение: Сделать исполняемым
chmod +x scripts/script_name.sh
```

### Проблема: SSH ошибка при деплое
```bash
# Решение: Проверить SSH подключение
ssh root@your-server-ip

# Добавить ключ в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

### Проблема: PM2 процессы не запускаются
```bash
# На сервере
pm2 logs  # Просмотр ошибок
pm2 restart all  # Перезапуск
pm2 delete all && pm2 start ecosystem.config.cjs  # Полный перезапуск
```

### Проблема: Nginx ошибки
```bash
# Проверка конфигурации
nginx -t

# Просмотр логов
tail -f /var/log/nginx/error.log

# Перезапуск
systemctl restart nginx
```

---

## 📚 Дополнительные ресурсы

- [Руководство по установке](../docs/SERVER_SETUP_GUIDE.md)
- [Руководство по развертыванию](../docs/DEPLOYMENT.md)
- [Технологический стек](../docs/TECH_STACK.md)
- [API документация](../docs/API.md)
- [Главная документация](../README.md)

---

## 🤝 Вклад в проект

Если вы хотите улучшить скрипты:
1. Форкните репозиторий
2. Создайте ветку для изменений
3. Протестируйте скрипт
4. Создайте Pull Request

---

**Версия:** 2.0.0  
**Дата обновления:** 2024  
**Статус:** Production Ready ✅
