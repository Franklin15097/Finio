#!/bin/bash

# Упрощенный скрипт для коммита изменений дизайна V3
# Без сборки (сборка выполняется на сервере)

set -e

echo "🎨 Коммит улучшений дизайна Finio V3..."

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m'

# Проверка изменений
if [ -z "$(git status --porcelain)" ]; then
    echo -e "${GREEN}✅ Все изменения уже закоммичены!${NC}"
    exit 0
fi

# Показать статус
echo -e "${BLUE}📊 Статус git:${NC}"
git status --short

# Добавить все изменения
git add .

# Создать коммит
git commit -m "🎨 Design V3: Дополнительные улучшения и фиксы"

# Push
if git remote | grep -q "origin"; then
    CURRENT_BRANCH=$(git branch --show-current)
    git push origin "$CURRENT_BRANCH"
    echo -e "${GREEN}✅ Изменения отправлены в репозиторий!${NC}"
fi

echo ""
echo -e "${GREEN}╔════════════════════════════════════════════╗${NC}"
echo -e "${GREEN}║  🎉 Коммит выполнен успешно!              ║${NC}"
echo -e "${GREEN}╚════════════════════════════════════════════╝${NC}"
echo ""
echo -e "${BLUE}📝 Следующие шаги:${NC}"
echo "  1. Выполните сборку на сервере: npm run build"
echo "  2. Перезапустите сервисы: pm2 restart all"
echo "  3. Проверьте приложение в браузере"
echo ""
