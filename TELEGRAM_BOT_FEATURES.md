# 🤖 Telegram Bot - Новые Функции

## Реализованные команды бота

### 📱 Основные команды

1. **`/start`** - Начать работу с Finio
   - Приветственное сообщение
   - Кнопки для открытия Mini App и сайта
   - Информация о быстрых командах

2. **`/add [сумма] [описание]`** - Быстрое добавление транзакции
   - Примеры:
     - `/add 500 продукты`
     - `/add 1000 зарплата`
     - `/add 150 кофе`
   - Интерактивный выбор типа (доход/расход)
   - Выбор категории через инлайн-кнопки
   - Возможность добавить без категории

3. **`/balance`** - Просмотр текущего баланса
   - Показывает текущий баланс
   - Общие доходы и расходы
   - Кнопка для открытия приложения

4. **`/app`** - Открыть Mini App
   - Быстрый доступ к приложению в Telegram

5. **`/site`** - Открыть сайт
   - Автоматическая авторизация через одноразовый токен

6. **`/help`** - Справка
   - Список всех команд
   - Примеры использования
   - Описание возможностей

7. **`/about`** - О приложении
   - Информация о Finio
   - Версия и функции

## 🎯 Инлайн-кнопки

### Выбор типа транзакции
После команды `/add` пользователь видит кнопки:
- 💸 Расход
- 💰 Доход

### Выбор категории
После выбора типа показываются категории пользователя:
- До 10 категорий с иконками
- Кнопка "Без категории"

### Подтверждение транзакции
После выбора категории:
- ✅ Транзакция добавлена
- Показывается сумма, тип и описание

## 🔧 Backend API

### Новые endpoints

1. **POST `/api/auth/telegram-user-token`**
   - Получение JWT токена по telegram_id
   - Используется ботом для выполнения операций от имени пользователя

2. **Существующие endpoints используемые ботом:**
   - `GET /api/dashboard/stats` - статистика баланса
   - `GET /api/categories` - список категорий
   - `POST /api/transactions` - добавление транзакции

## 📊 UI/UX Улучшения

### Круговые индикаторы баланса
На главной странице Telegram Mini App добавлены 3 круговых индикатора:

1. **Доходы** (зелёный)
   - Процент доходов от общего оборота
   - Визуальная шкала прогресса

2. **Баланс** (фиолетовый/красный)
   - Текущий баланс относительно оборота
   - Цвет меняется в зависимости от знака

3. **Счета** (синий)
   - Фактический баланс относительно планового
   - Показывает выполнение плана

### Компонент CircularProgress
- Настраиваемый размер и цвет
- Плавная анимация
- Процентное отображение
- Подпись под индикатором

## 🚀 Как использовать

### Для пользователей

1. Найдите бота @FinanceStudio_bot в Telegram
2. Нажмите `/start`
3. Используйте быстрые команды:
   ```
   /add 500 продукты
   /balance
   /app
   ```

### Для разработчиков

1. Бот работает через long polling
2. Обрабатывает как текстовые сообщения, так и callback queries
3. Использует JWT токены для авторизации
4. Интегрирован с основным backend API

## 📝 Технические детали

### Обработка команд
```typescript
// Команда /add
if (text.startsWith('/add ')) {
  // Парсинг суммы и описания
  // Показ кнопок выбора типа
  // Показ категорий
  // Добавление транзакции
}
```

### Callback queries
```typescript
// Обработка нажатий на кнопки
if (data.startsWith('add_expense_') || data.startsWith('add_income_')) {
  // Извлечение параметров
  // Добавление транзакции
  // Обновление сообщения
}
```

### Авторизация
```typescript
// Получение токена для бота
const token = await getUserToken(telegramId);

// Использование токена для API запросов
const response = await fetch(`${BACKEND_URL}/api/transactions`, {
  headers: {
    'Authorization': `Bearer ${token}`,
  },
});
```

## 🎨 Визуальные улучшения

### Главная страница (TelegramBalance)
- ✅ Круговые индикаторы финансового здоровья
- ✅ Sparkline графики в карточках
- ✅ Градиентные иконки
- ✅ Анимированные переходы

### Карточки статистики
- Градиентные фоны
- Иконки с тенями
- Мини-графики трендов
- Цветовая индикация (зелёный/красный)

## 🔮 Будущие улучшения

### Планируется добавить:
- [ ] Push-уведомления через бота
- [ ] Напоминания о регулярных платежах
- [ ] Виджеты для быстрого просмотра баланса
- [ ] Команды для просмотра аналитики
- [ ] Экспорт данных через бота
- [ ] Настройка бюджетов через команды

## 📦 Деплой

Бот запускается отдельным процессом через PM2:
```bash
pm2 start dist/bot.js --name finio-bot
```

Или через npm script:
```bash
npm run bot
```

## 🔐 Безопасность

- Все команды требуют авторизации
- Токены имеют срок действия 30 дней
- Одноразовые auth токены для сайта (5 минут)
- Валидация Telegram данных через HMAC

## 📞 Поддержка

Если возникли вопросы:
- Используйте `/help` в боте
- Напишите на support@studiofinance.ru
- Откройте issue на GitHub
