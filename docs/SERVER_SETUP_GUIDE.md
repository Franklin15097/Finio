# 🚀 Руководство по автоматической установке Finio на сервер

Это руководство описывает процесс полной автоматической установки и настройки проекта Finio на чистом сервере с помощью скрипта `full_server_setup.sh`.

## 📋 Что делает скрипт

Скрипт `scripts/full_server_setup.sh` выполняет полную автоматизированную установку:

### 1. Очистка сервера
- Остановка всех сервисов
- Удаление старых файлов проекта
- Очистка базы данных
- Очистка Redis кэша

### 2. Установка системных зависимостей
- Node.js 18+
- MySQL 8.0+
- Redis 6+
- Nginx
- PM2
- Git
- Certbot (SSL)
- UFW (Firewall)
- Fail2Ban

### 3. Настройка базы данных
- Создание базы данных
- Создание пользователя
- Импорт схемы с индексами
- Оптимизация параметров MySQL

### 4. Развертывание приложения
- Клонирование репозитория
- Установка зависимостей
- Сборка frontend и backend
- Создание конфигурационных файлов

### 5. Настройка веб-сервера
- Конфигурация Nginx
- Настройка reverse proxy
- Поддержка WebSocket
- Gzip сжатие
- Кэширование статики

### 6. Настройка безопасности
- SSL сертификаты (Let's Encrypt)
- Firewall правила
- Fail2Ban защита
- Rate limiting

### 7. Автоматизация
- Автозапуск сервисов
- Ежедневные бэкапы БД
- Автообновление SSL
- Мониторинг процессов

---

## 🔧 Подготовка к установке

### Требования к серверу
- **ОС:** Ubuntu Server 20.04+ (или совместимый Linux)
- **RAM:** Минимум 2GB
- **Диск:** Минимум 20GB свободного места
- **Доступ:** SSH с правами root/sudo
- **Домены:** 2 домена с настроенными DNS записями

### Необходимая информация

Перед запуском скрипта подготовьте:

1. **Telegram Bot Token**
   - Получите от [@BotFather](https://t.me/BotFather) в Telegram
   - Команда: `/newbot`
   - Сохраните токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Домены**
   - Основной домен для frontend (например: `studiofinance.ru`)
   - Поддомен для API (например: `api.studiofinance.ru`)
   - Оба домена должны указывать на IP вашего сервера (A-записи)

3. **Email для SSL**
   - Действующий email для регистрации SSL сертификатов

---

## 📥 Установка

### Шаг 1: Подключение к серверу

```bash
ssh root@your-server-ip
```

### Шаг 2: Скачивание скрипта

```bash
# Скачать только скрипт установки
wget https://raw.githubusercontent.com/Franklin15097/Finio/main/scripts/full_server_setup.sh

# Или клонировать весь репозиторий
git clone https://github.com/Franklin15097/Finio.git
cd Finio
```

### Шаг 3: Настройка параметров

Откройте скрипт в редакторе:

```bash
nano scripts/full_server_setup.sh
```

Найдите секцию **КОНФИГУРАЦИЯ** и измените значения:

```bash
# Домены
DOMAIN="studiofinance.ru"              # ← Ваш основной домен
API_DOMAIN="api.studiofinance.ru"      # ← Ваш API домен

# Telegram Bot Token (ОБЯЗАТЕЛЬНО!)
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"  # ← Ваш токен

# Email для SSL сертификатов (ОБЯЗАТЕЛЬНО!)
SSL_EMAIL="your-email@example.com"     # ← Ваш email

# Остальные параметры можно оставить по умолчанию
# Пароли будут сгенерированы автоматически
```

Сохраните файл: `Ctrl+X`, затем `Y`, затем `Enter`

### Шаг 4: Запуск установки

```bash
# Сделать скрипт исполняемым
chmod +x scripts/full_server_setup.sh

# Запустить установку (требуются права root)
sudo ./scripts/full_server_setup.sh
```

### Шаг 5: Ожидание завершения

Установка займет **15-30 минут** в зависимости от скорости сервера и интернета.

Скрипт выведет подробную информацию о каждом шаге:
- ✓ Зеленая галочка - успешно
- ✗ Красный крестик - ошибка
- → Синяя стрелка - информация
- ⚠ Желтый треугольник - предупреждение

---

## ✅ После установки

### Проверка установки

После завершения скрипт выведет:
- URLs приложения
- Учетные данные базы данных
- JWT Secret
- Полезные команды

### Сохраненные учетные данные

Все учетные данные сохранены в файле:
```
/var/www/studiofinance/CREDENTIALS.txt
```

Просмотр:
```bash
cat /var/www/studiofinance/CREDENTIALS.txt
```

⚠️ **ВАЖНО:** Сохраните этот файл в безопасном месте и удалите с сервера!

### Проверка работы сервисов

```bash
# Статус PM2 процессов
pm2 status

# Логи приложения
pm2 logs

# Статус Nginx
systemctl status nginx

# Статус MySQL
systemctl status mysql

# Статус Redis
systemctl status redis-server

# Проверка портов
netstat -tlnp | grep -E ':(80|443|5000)'
```

### Открытие приложения

1. Откройте в браузере: `https://your-domain.com`
2. Приложение должно загрузиться
3. Зарегистрируйтесь или войдите через Telegram

---

## 🔧 Управление приложением

### PM2 команды

```bash
# Просмотр статуса
pm2 status

# Просмотр логов
pm2 logs                    # Все логи
pm2 logs finio-backend      # Только backend
pm2 logs finio-bot          # Только bot

# Перезапуск
pm2 restart all             # Все процессы
pm2 restart finio-backend   # Только backend
pm2 restart finio-bot       # Только bot

# Остановка
pm2 stop all
pm2 stop finio-backend

# Мониторинг в реальном времени
pm2 monit
```

### Nginx команды

```bash
# Проверка конфигурации
nginx -t

# Перезапуск
systemctl restart nginx

# Просмотр логов
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### MySQL команды

```bash
# Подключение к БД
mysql -u finio -p financial_db

# Просмотр таблиц
mysql -u finio -p -e "USE financial_db; SHOW TABLES;"

# Бэкап вручную
/usr/local/bin/finio-backup.sh
```

---

## 🔄 Обновление приложения

### Автоматическое обновление

```bash
cd /var/www/studiofinance
git pull
cd backend && npm install && npm run build
cd ../frontend && npm install && npm run build
pm2 restart all
```

### Использование deploy скрипта

Если у вас есть SSH доступ с локальной машины:

```bash
# На локальной машине
./scripts/deploy.sh full
```

---

## 💾 Резервное копирование

### Автоматические бэкапы

Скрипт настраивает автоматические ежедневные бэкапы:
- **Время:** 3:00 ночи
- **Директория:** `/var/backups/finio/`
- **Хранение:** 30 дней
- **Формат:** `backup_YYYYMMDD_HHMMSS.sql.gz`

### Ручной бэкап

```bash
# Создать бэкап
/usr/local/bin/finio-backup.sh

# Просмотр бэкапов
ls -lh /var/backups/finio/

# Восстановление из бэкапа
gunzip < /var/backups/finio/backup_20240101_030000.sql.gz | mysql -u finio -p financial_db
```

---

## 🔒 Безопасность

### Firewall (UFW)

```bash
# Статус
ufw status

# Разрешить порт
ufw allow 8080/tcp

# Заблокировать IP
ufw deny from 1.2.3.4
```

### Fail2Ban

```bash
# Статус
fail2ban-client status

# Разблокировать IP
fail2ban-client set sshd unbanip 1.2.3.4
```

### SSL сертификаты

```bash
# Проверка срока действия
certbot certificates

# Ручное обновление
certbot renew

# Тест обновления
certbot renew --dry-run
```

---

## 🐛 Устранение неполадок

### Приложение не запускается

```bash
# Проверить логи PM2
pm2 logs --lines 100

# Проверить .env файл
cat /var/www/studiofinance/backend/.env

# Перезапустить
pm2 restart all
```

### Ошибки базы данных

```bash
# Проверить подключение
mysql -u finio -p -e "SELECT 1;"

# Проверить таблицы
mysql -u finio -p financial_db -e "SHOW TABLES;"

# Переимпортировать схему
mysql -u finio -p financial_db < /var/www/studiofinance/backend/database/schema_improved.sql
```

### Nginx ошибки

```bash
# Проверить конфигурацию
nginx -t

# Проверить логи
tail -f /var/log/nginx/error.log

# Перезапустить
systemctl restart nginx
```

### Redis не работает

```bash
# Проверить статус
systemctl status redis-server

# Проверить подключение
redis-cli ping

# Перезапустить
systemctl restart redis-server
```

### SSL проблемы

```bash
# Проверить сертификаты
certbot certificates

# Проверить DNS
dig your-domain.com
dig api.your-domain.com

# Переполучить сертификаты
certbot --nginx -d your-domain.com -d api.your-domain.com --force-renewal
```

---

## 📊 Мониторинг

### Системные ресурсы

```bash
# CPU и память
htop

# Использование диска
df -h

# Сетевые подключения
netstat -tulpn
```

### Логи приложения

```bash
# PM2 логи
pm2 logs --lines 100

# Nginx логи
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# MySQL логи
tail -f /var/log/mysql/error.log
tail -f /var/log/mysql/slow-query.log

# Системные логи
journalctl -u nginx -f
journalctl -u mysql -f
```

---

## 📞 Поддержка

### Документация
- [README.md](../README.md) - Общая информация
- [TECH_STACK.md](TECH_STACK.md) - Технологический стек
- [DEPLOYMENT.md](DEPLOYMENT.md) - Ручное развертывание
- [API.md](API.md) - API документация

### Проблемы
Если возникли проблемы:
1. Проверьте логи (см. раздел "Устранение неполадок")
2. Убедитесь, что все сервисы запущены
3. Проверьте DNS записи доменов
4. Создайте issue на GitHub

---

## ✨ Дополнительные возможности

### Настройка Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Укажите URL: `https://your-domain.com`

### Настройка домена для Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/setdomain`
3. Выберите вашего бота
4. Укажите домен: `your-domain.com`

---

**Версия:** 2.0  
**Дата:** 2024  
**Статус:** Production Ready ✅
