# 🛠️ Технологический стек проекта Finio

Полный список всех технологий, зависимостей и требований для развертывания проекта на хостинге.

## 📋 Содержание
- [Системные требования](#системные-требования)
- [Frontend технологии](#frontend-технологии)
- [Backend технологии](#backend-технологии)
- [База данных](#база-данных)
- [Внешние сервисы](#внешние-сервисы)
- [Инфраструктура](#инфраструктура)
- [Переменные окружения](#переменные-окружения)

---

## 🖥️ Системные требования

### Операционная система
- **Ubuntu Server 20.04+** (рекомендуется) или совместимый Linux дистрибутив
- Минимум 2GB RAM
- Минимум 20GB свободного места на диске
- SSH доступ с правами sudo

### Обязательное ПО

#### 1. Node.js
- **Версия:** 18.x или выше
- **Установка:**
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```
- **Проверка:** `node --version` (должно быть >= 18.0.0)

#### 2. MySQL
- **Версия:** 8.0 или выше
- **Установка:**
```bash
sudo apt install -y mysql-server
```
- **Проверка:** `mysql --version`
- **Требования:** 
  - Поддержка InnoDB engine
  - UTF8MB4 character set
  - Минимум 512MB buffer pool

#### 3. Redis
- **Версия:** 6.0 или выше (НОВОЕ в v2.0!)
- **Установка:**
```bash
sudo apt install -y redis-server
```
- **Проверка:** `redis-cli --version`
- **Использование:** Кэширование запросов, хранение токенов

#### 4. Nginx
- **Версия:** 1.18 или выше
- **Установка:**
```bash
sudo apt install -y nginx
```
- **Использование:** Reverse proxy, статические файлы, SSL termination

#### 5. PM2
- **Версия:** Последняя стабильная
- **Установка:**
```bash
sudo npm install -g pm2
```
- **Использование:** Управление процессами Node.js

#### 6. Git
- **Версия:** 2.x или выше
- **Установка:**
```bash
sudo apt install -y git
```

#### 7. Certbot (для SSL)
- **Установка:**
```bash
sudo apt install -y certbot python3-certbot-nginx
```
- **Использование:** Автоматическое получение SSL сертификатов Let's Encrypt

---

## 🎨 Frontend технологии

### Основной стек
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **React** | 18.2.0 | UI фреймворк |
| **TypeScript** | 5.3.3 | Типизация |
| **Vite** | 5.0.8 | Сборщик и dev-сервер |
| **React Router DOM** | 6.21.0 | Маршрутизация |

### Стилизация
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **TailwindCSS** | 3.4.0 | Utility-first CSS |
| **PostCSS** | 8.4.32 | CSS обработка |
| **Autoprefixer** | 10.4.16 | Вендорные префиксы |

### Визуализация данных
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Recharts** | 2.10.3 | Графики и диаграммы |
| **Lucide React** | 0.294.0 | Иконки |

### Утилиты
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Date-fns** | 3.0.6 | Работа с датами |
| **Socket.io-client** | 4.7.2 | WebSocket клиент |

### Команды сборки
```bash
npm install          # Установка зависимостей
npm run dev          # Разработка (порт 5173)
npm run build        # Продакшн сборка
npm run preview      # Предпросмотр сборки
```

---

## ⚙️ Backend технологии

### Основной стек
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Node.js** | 18+ | Runtime окружение |
| **Express** | 4.18.2 | Web фреймворк |
| **TypeScript** | 5.3.3 | Типизация |
| **tsx** | 4.7.0 | TypeScript execution |

### База данных
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **MySQL2** | 3.6.5 | MySQL драйвер |

### Аутентификация и безопасность
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **jsonwebtoken** | 9.0.2 | JWT токены |
| **bcryptjs** | 2.4.3 | Хеширование паролей |
| **express-validator** | 7.0.1 | Валидация входных данных |
| **zod** | 3.22.4 | Schema валидация (v2.0) |
| **express-rate-limit** | 7.1.5 | Защита от DDoS (v2.0) |

### Real-time и кэширование
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **Socket.io** | 4.7.2 | WebSocket сервер (v2.0) |
| **ioredis** | 5.3.2 | Redis клиент (v2.0) |

### Экспорт данных
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **xlsx** | 0.18.5 | Excel файлы |
| **pdfkit** | 0.14.0 | PDF документы |

### Логирование
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **morgan** | 1.10.0 | HTTP логи |
| **winston** | 3.11.0 | Структурированное логирование (v2.0) |

### Прочее
| Технология | Версия | Назначение |
|-----------|--------|-----------|
| **cors** | 2.8.5 | CORS middleware |
| **dotenv** | 16.3.1 | Переменные окружения |
| **node-fetch** | 3.3.2 | HTTP клиент для Telegram API |

### Команды запуска
```bash
npm install          # Установка зависимостей
npm run dev          # Разработка (порт 5000)
npm run build        # Компиляция TypeScript
npm start            # Запуск API сервера
npm run bot          # Запуск Telegram бота
```

---

## 🗄️ База данных

### MySQL 8.0+

#### Схема базы данных
- **5 основных таблиц:**
  1. `users` - Пользователи (с Telegram интеграцией)
  2. `accounts` - Финансовые счета
  3. `categories` - Категории доходов/расходов
  4. `transactions` - Транзакции
  5. `budgets` - Бюджеты

#### Особенности
- **Engine:** InnoDB (поддержка транзакций и внешних ключей)
- **Character Set:** UTF8MB4 (полная поддержка Unicode, включая эмодзи)
- **Collation:** utf8mb4_unicode_ci
- **Индексы:** Оптимизированные составные индексы для быстрых запросов
- **Foreign Keys:** Каскадное удаление для целостности данных

#### Файлы схемы
- `backend/database/schema.sql` - Базовая схема
- `backend/database/schema_improved.sql` - Оптимизированная схема с индексами (рекомендуется)
- `backend/database/telegram_migration.sql` - Миграция для Telegram интеграции

#### Производительность
- Время ответа: 10-50ms (с индексами)
- Поддержка до 100,000+ транзакций на пользователя
- Оптимизированные запросы с JOIN

---

## 🌐 Внешние сервисы

### 1. Telegram Bot API
- **Назначение:** Telegram Mini App и бот интеграция
- **Требования:** 
  - Bot Token от @BotFather
  - Webhook или polling режим (используется polling)
- **Команды бота:**
  - `/start` - Регистрация/вход
  - `/add` - Добавить транзакцию
  - `/balance` - Показать баланс
  - `/app` - Открыть Mini App
  - `/site` - Ссылка на веб-версию
  - `/help` - Помощь
  - `/about` - О приложении

### 2. Redis
- **Назначение:** 
  - Кэширование результатов запросов (TTL: 60-300 сек)
  - Хранение одноразовых auth токенов
  - Session management
- **Производительность:** Ускорение запросов в 10-20 раз
- **Порт:** 6379 (по умолчанию)

### 3. WebSocket (Socket.io)
- **Назначение:** Real-time синхронизация между веб и Telegram
- **События:**
  - `transaction_created` - Новая транзакция
  - `category_updated` - Обновление категории
  - `account_changed` - Изменение счета
  - `budget_modified` - Изменение бюджета
- **Аутентификация:** JWT токен при подключении
- **Комнаты:** User-specific rooms (`user:${userId}`)

---

## 🏗️ Инфраструктура

### Архитектура развертывания

```
Internet
    ↓
[Nginx] (Port 80/443)
    ↓
    ├─→ Frontend (Static files)
    ├─→ Backend API (Proxy to :5000)
    └─→ WebSocket (Upgrade headers)
         ↓
    [PM2 Process Manager]
         ↓
         ├─→ finio-backend (Node.js :5000)
         └─→ finio-bot (Node.js)
              ↓
         [MySQL] (Port 3306)
         [Redis] (Port 6379)
```

### Nginx конфигурация
- **Reverse proxy** для API запросов
- **Static file serving** для frontend
- **WebSocket support** (Upgrade headers)
- **SSL/TLS termination**
- **Gzip compression**
- **Кэширование статики** (1 год)
- **SPA routing** (fallback на index.html)

### PM2 конфигурация
```javascript
// ecosystem.config.cjs
{
  apps: [
    {
      name: 'finio-backend',
      script: './dist/index.js',
      instances: 1,
      max_memory_restart: '1G',
      env: { NODE_ENV: 'production' }
    },
    {
      name: 'finio-bot',
      script: './dist/bot.js',
      instances: 1,
      max_memory_restart: '500M',
      env: { NODE_ENV: 'production' }
    }
  ]
}
```

### Безопасность

#### Firewall (UFW)
```bash
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP
sudo ufw allow 443/tcp   # HTTPS
sudo ufw enable
```

#### Fail2Ban
- Защита от brute-force атак на SSH
- Автоматическая блокировка IP после неудачных попыток

#### Rate Limiting
- API: 100 запросов / 15 минут
- Auth: 10 попыток / 15 минут
- Create: 30 операций / минуту
- Export: 10 экспортов / час

---

## 🔐 Переменные окружения

### Backend (.env)
```env
# Сервер
PORT=5000
NODE_ENV=production

# База данных
DB_HOST=localhost
DB_USER=finio
DB_PASSWORD=<secure_password>
DB_NAME=financial_db

# Безопасность
JWT_SECRET=<long_random_string_min_32_chars>

# Telegram
TELEGRAM_BOT_TOKEN=<bot_token_from_botfather>

# URLs
BACKEND_URL=https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Redis (NEW в v2.0)
REDIS_URL=redis://localhost:6379
```

### Frontend (.env)
```env
VITE_API_URL=https://api.yourdomain.com
```

### Генерация секретов
```bash
# JWT Secret (32+ символов)
openssl rand -base64 32

# MySQL пароль
openssl rand -base64 24
```

---

## 📦 Порты

| Сервис | Порт | Доступ |
|--------|------|--------|
| Nginx HTTP | 80 | Публичный |
| Nginx HTTPS | 443 | Публичный |
| Backend API | 5000 | Локальный (через Nginx) |
| MySQL | 3306 | Локальный |
| Redis | 6379 | Локальный |
| SSH | 22 | Публичный (защищен) |

---

## 🔄 Процесс сборки

### Frontend
```bash
cd frontend
npm install
npm run build
# Результат: frontend/dist/
```

### Backend
```bash
cd backend
npm install
npm run build
# Результат: backend/dist/
```

---

## 📊 Мониторинг и логи

### PM2 мониторинг
```bash
pm2 status              # Статус процессов
pm2 logs                # Просмотр логов
pm2 monit               # Real-time мониторинг
pm2 restart all         # Перезапуск всех процессов
```

### Логи
- **Backend:** `backend/logs/` (Winston)
- **Nginx:** `/var/log/nginx/`
- **MySQL:** `/var/log/mysql/`
- **PM2:** `~/.pm2/logs/`

---

## 💾 Резервное копирование

### MySQL бэкапы
```bash
# Ежедневный бэкап (cron)
mysqldump -u finio -p financial_db > backup_$(date +%Y%m%d).sql

# Восстановление
mysql -u finio -p financial_db < backup_20240101.sql
```

### Ротация бэкапов
- Хранение: 30 дней
- Автоматическое удаление старых бэкапов

---

## 🚀 Производительность (v2.0)

### Оптимизации
- **Redis кэширование:** 10-20x ускорение запросов
- **Индексы БД:** 200ms → 10-50ms
- **WebSocket:** <100ms latency для real-time обновлений
- **Gzip сжатие:** Уменьшение размера ответов на 70%
- **Static caching:** 1 год для неизменяемых ресурсов

### Метрики
- Время ответа API: 10-50ms (средн.)
- WebSocket latency: <100ms
- Одновременные подключения: 1000+
- Throughput: 100+ req/sec

---

## 📚 Дополнительные ресурсы

- [Руководство по установке](../INSTALLATION.md)
- [Руководство по развертыванию](DEPLOYMENT.md)
- [API документация](API.md)
- [Улучшения v2.0](IMPROVEMENTS.md)
- [WebSocket использование](WEBSOCKET_USAGE.md)

---

**Версия документа:** 2.0  
**Дата обновления:** 2024  
**Статус:** Production Ready ✅
